{
  "name": "sprint-planner-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Testes end-to-end (E2E) do Sprint Planner com Cypress. Sobem backend (banco FAKE em memoria) + frontend (Vite) e dirigem o navegador como um usuario real.",
  "scripts": {
    "//": "----- scripts de E2E (explicacao em PT-BR) -----",
    "e2e:backend": "cross-env USE_FAKE_DB=1 PORT=3001 node backend/src/server.js",
    "e2e:frontend": "npm --prefix frontend run dev -- --port 5173 --host",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "//e2e": "Sobe backend (fake) + frontend e roda o cypress headless, tudo de uma vez. So funciona com start-server-and-test instalado.",
    "e2e": "start-server-and-test e2e:backend http://localhost:3001/api/health e2e:frontend http://localhost:5173 cy:run"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "cypress": "^13.15.0",
    "start-server-and-test": "^2.0.8"
  }
}
